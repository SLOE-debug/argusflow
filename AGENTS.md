# ArgusFlow 仓库协作规范

## 核心原则

- 强类型优先。类型必须表达真实的领域约束、输入输出关系和状态边界，不以散落字符串、弱类型容器或隐式约定代替模型。
- 高内聚、低耦合是强制架构约束；模块、文件或组件职责边界不清时，必须先拆分再继续实现。
- 入口文件只负责注册、装配和编排，不承载转换、映射、构造、解析、校验、持久化、渲染或具体业务细节。
- 对外只暴露稳定、最小且尽可能只读的契约；内部可变状态和实现细节不得被调用方任意修改。
- 不为了追求“通用”提前制造空抽象；泛型、Trait、接口和注册表必须表达真实复用关系或依赖边界。

## TypeScript 开发规范

- 代码以强类型为先，优先使用接口、泛型、枚举、只读类型、映射类型、条件类型和明确的领域模型。
- 充分使用 TypeScript 类型推导、泛型约束、`satisfies`、判别联合、类型守卫和类型化注册表，避免 `any`、无约束类型断言、弱类型服务定位和字符串式分发。
- 运行时协议、业务状态、节点类型、事件类型和注册键不得写成散落字符串；应来自枚举、判别联合、只读清单或类型化 key。
- 每个模块和 React 组件只承担一种明确职责；状态编排、数据转换、业务决策、画布交互和视觉渲染不得长期混在同一组件或文件中。
- 跨模块调用必须依赖稳定接口、领域类型或门面；底层通用模块不得反向依赖具体业务实现，同层业务模块不得绕过公共契约互相读取内部状态。
- React 组件优先通过明确的 `Props` 类型和回调契约通信；可派生状态不得重复存储，可复用状态逻辑应下沉到 Hook，可复用纯转换应下沉到普通模块。
- 数组、对象和配置表在不需要修改时使用 `readonly`、`ReadonlyArray` 或 `as const`；不得把可变集合直接暴露为公共契约。
- JSX 必须保持可读：多属性元素分行书写，每个属性独占一行；复杂条件、长回调和重复结构必须提取为具名变量、函数或子组件，禁止持续堆叠超长单行 TSX。
- Tailwind `className` 可以保留在组件内，但长类名应按组件层级合理换行；不得为了缩短行宽退回普通 CSS，也不得把多个职责压缩成难以审查的一行。

## TypeScript 模块边界

- `src/flow` 只保存与业务无关的画布内核、几何、路由、选择和状态契约，不得 import `features` 或具体工作流组件。
- `src/features` 保存工作流领域模型、API 和状态编排，不得承载具体页面布局或视觉样式。
- `src/components` 通过 `flow` 与 `features` 的稳定契约渲染业务界面；组件不得绕过公开状态操作直接修改 Store 内部结构。
- `Input`、`Select`、`Textarea`、`Checkbox`、`Radio`、`Switch`、`FormField` 等通用表单组件必须放在 `src/components/ui` 下；业务组件不得重复实现同类基础控件或复制其公共样式。
- `src/components/ui` 只保存与业务无关的强类型 UI 基础组件，不得 import `features`、`flow` 或具体工作流组件；业务差异通过明确的 Props 和组合内容传入。
- 不同 Feature 之间禁止直接读取内部实现；跨 Feature 通信必须经过类型化契约、门面、事件或注册表。

## Rust 开发规范

- Rust 同样以强类型为先，优先使用 `struct`、`enum`、Trait、泛型、关联类型、Newtype 和明确的领域模型表达约束，不用字符串、裸数字或无语义元组模拟业务类型。
- 有限状态、事件、命令、节点类型和错误种类必须优先建模为枚举，并使用穷尽 `match` 处理；不得依赖字符串比较或默认分支吞掉新增状态。
- 不变量应尽量在构造阶段建立；需要校验的领域值应通过私有字段、构造函数或 `TryFrom`/`FromStr` 形成有效类型，避免无效状态在系统中传播。
- 可失败操作必须返回明确的 `Result`，可缺失值使用 `Option`；生产代码不得随意 `unwrap`、`expect` 或 `panic!`，只有测试或已证明的不变量可以使用，并应说明依据。
- 所有权和借用关系必须清晰。默认不可变，优先传递引用、切片和迭代器；不得为了绕开借用设计而无意义 `clone`，共享可变状态必须限制在明确的并发或生命周期边界内。
- Trait 只用于真实行为边界或多实现契约；能使用静态分发时优先泛型，只有确需运行时多态时使用 `dyn Trait`，不得提前制造空 Trait 或单实现抽象层。
- 模块必须保持高内聚、低耦合：领域模型、校验、执行、平台适配和 I/O 分层放置；核心 crate 不得反向依赖 Tauri、Windows 或具体平台实现。
- `lib.rs`、`main.rs` 和 `mod.rs` 优先只保留模块声明、公共导出和启动编排；复杂实现应进入具名模块。
- 公共 API 必须最小化，优先 `pub(crate)` 或私有；公开结构的字段应谨慎暴露，优先通过只读访问器和领域方法维护不变量。
- 并发边界应由类型体现，明确 `Send`、`Sync`、任务所有权和取消语义；不得用全局可变状态或隐式锁顺序传递协议。
- `unsafe` 只能封装无法由安全 Rust 表达的最小边界，必须记录安全不变量，并向外提供安全接口；不得扩大 `unsafe` 作用域以图省事。
- 热路径应避免重复分配、无意义克隆和临时集合；优化必须基于明确的调用频率和数据规模，不得牺牲领域边界换取未经验证的微优化。

## Rust crate 边界

- `argusflow-core` 只保存跨平台领域模型、动作契约、错误和执行事件，不得依赖 Tauri、Windows API、浏览器或具体运行时实现。
- `argusflow-runtime` 可以依赖 `argusflow-core` 编排校验、调度和执行，但不得反向把运行时状态泄漏到核心领域模型。
- Windows、浏览器、视觉和 Agent crate 通过核心 Trait 与类型协作，不得互相静态依赖具体实现或复制协议结构。
- `src-tauri` 只负责桌面应用装配、命令适配和状态托管；领域校验和执行规则必须下沉到对应 crate。

## 实现规范

- 单文件超过 500 行前必须先评估并按职责拆分；入口文件和公共导出文件必须优先保持精简。
- 不为了减少补丁次数把 helper、领域模型、转换、解析、校验或工具逻辑临时塞进当前文件。
- 除非用户明确要求，不添加兼容分支、回退逻辑、双写逻辑或新旧字段映射。
- 不主动运行编译或构建命令；编译和构建由用户自行负责。

## 前端样式

- **前端样式必须优先使用 Tailwind CSS（twcss）工具类。** 布局、尺寸、字号、间距、颜色、边框、圆角、阴影、Flex/Grid、对齐、响应式和常规交互状态不得新写普通 CSS。
- **除非样式非常复杂且 Tailwind CSS 无法清晰、可靠地表达，否则禁止新增普通 CSS。** 允许例外的场景仅限复杂 SVG 绘制、关键帧动画、必须依赖伪元素的复杂效果等，并应将原生 CSS 控制在最小范围内。
- 不得为了缩短 `className`、沿用旧习惯或追求表面整洁，把本可直接使用 Tailwind CSS 表达的样式抽成普通 CSS 类或新增 CSS 文件。
- 修改已有前端界面时，应同步评估所触及的普通 CSS；能够直接迁移为 Tailwind CSS 的部分应随本次改动迁移，不得继续扩张普通 CSS。

## 注释

- 除了名称足够直白、无需额外上下文就能准确理解用途的变量之外，变量声明都应补充注释。
- 即使变量名称表面可读，只要名称较长、语义拗口、依赖业务上下文、单位或取值范围不明确，或者可能产生二义性，也必须添加注释。
- TypeScript/JavaScript 优先使用 `/** ... */` 风格的详细注释；Rust 使用对应的文档注释 `///`、`//!`，局部实现细节可使用 `//`。
- 对公开类型、接口、函数、方法、字段、常量和模块添加文档注释，说明其用途、关键约束、输入输出以及必要的副作用；不要只把标识符名称换一种说法。
- 对复杂分支、状态转换、算法意图、边界条件和不直观的实现选择添加解释性注释，重点说明“为什么”，而非逐行复述“做了什么”。
- 简单循环索引、明显的布尔判断结果、惯用回调参数等一眼可懂的局部变量可以不注释，避免制造噪声。
- 注释必须与当前实现保持一致；修改代码时同步更新相关注释，不保留失效、猜测性或误导性描述。

## UI 与目录重构规则

- 通用按钮、图标按钮、分裂按钮、表单字段、下拉菜单、提示框和确认对话框统一从 `src/components/ui` 复用；业务组件不得重复实现同类基础控件。
- 业务组件原则上不得新增裸 `<button>`、`<input>`、`<select>`、`<textarea>`；仅保留标签页、画布交互和其他具有明确业务语义的例外，并通过注释说明原因。
- `src/components/workflow` 按工作区、工具栏、停靠面板、检查器、节点字段、节点面板、画布和执行视图拆分；不得把不同职责重新堆回入口文件。
- `src/features/workflow` 按模型、API、Studio 编排、节点、值表达式、组件、执行和输入契约拆分；页面布局与视觉样式放在 `src/components`。
- `src/flow` 只保留业务无关的画布内核、交互、状态、路由、几何、选择和视口模块；不得依赖 `features` 或工作流组件。
- `argusflow-runtime` 按执行、校验、组件、资源和命令职责组织模块；`lib.rs` 只负责模块声明与稳定公共导出。
- 跨目录引用优先通过对应目录的 `index.ts` 或 Rust 模块公开导出；模块内部实现才直接引用同一职责目录的具体文件。
- 目录迁移应通过 `scripts/refactor-layout.mjs` 或 `scripts/refactor-rust-layout.py` 的显式映射执行，并保持脚本可重复运行且不改变协议和算法语义。
