# ArgusFlow 代理工作入口

## 必读规则

在开始本仓库的任何工作前，必须使用文件读取工具完整读取并遵守以下文件，路径均相对于本 AGENTS.md 所在的仓库根目录：

1. [通用编码与架构规范](docs/rules/coding-standards.md)：前后端共同遵守的适度抽象、高可用、高内聚、低耦合、稳定契约与注释要求。
2. [前端规范](docs/rules/frontend.md)：TypeScript、React、UI、样式、前端模块边界与异步状态。
3. [后端规范](docs/rules/backend.md)：Rust、能力模块职责边界、平台适配、执行与资源生命周期。
4. [协作规范](docs/rules/collaboration.md)：兼容性、文件体积、文件操作、验证与 Git 提交规则。

这些文件是本仓库工作指令的一部分，不是可选参考。不要只读取链接标题或把链接视为已经加载；若读取结果被截断，应继续读取剩余内容。已经完整读取且未发生变化的文件无需重复读取。用户的明确要求优先于仓库规则。

## 重构上下文

当前 main 从独立的空提交开始重构；experimental 保留旧实现与完整历史，作为参考。
编码规范中的目录与 crate 边界在涉及对应模块时适用，不表示要求一次性恢复全部旧目录。
旧目录迁移脚本的规则适用于已有实现的目录迁移；从零创建模块不属于目录迁移。

## 源码层级与测试目录（强制）

- 源码按能力和职责建立目录，允许继续细分任意层级；不得把所有实现文件平铺在 `src/`。目录深度由职责决定，不创建没有实际职责的空层级。
- `lib.rs`、`main.rs`、`mod.rs` 只保留模块声明、导出和必要编排；配置、校验、算法、平台适配和资源管理分别进入对应职责文件。
- **全仓库测试统一放在根目录 `tests/`，不得散落在 `src/`、各 crate 自己的 `tests/` 或其他位置。** Rust 测试按 `tests/<crate 名>/unit/`、`integration/`、`support/`、`fixtures/` 组织，内部再按被测模块分层。
- 单元测试函数、测试替身、测试辅助函数、验收窗口和测试图片都遵守上述位置；禁止在生产文件内写 `mod tests { ... }`、`#[test]`、`#[tokio::test]` 或测试辅助实现。
- 需要访问私有实现的单元测试，生产模块仅保留 `#[cfg(test)]`、指向根目录测试文件的 `#[path = "..."]` 和 `mod tests;` 声明；不得为了移动测试扩大生产 API 可见性或新增兼容导出。
- Rust 集成测试在所属 crate 的 `Cargo.toml` 用 `[[test]]` 显式指向根目录文件，并设置 `autotests = false`；测试专用可执行示例也用显式 `[[example]]` 指向 `tests/<crate 名>/support/`。
- 调整目录时同步更新模块引用、Cargo 入口、测试资源路径和文档；运行格式、Clippy 与工作区测试，核对测试数量及 ignored 项，防止文件搬走后测试被漏编译。目录图见 [项目结构](docs/layout.md)。

## 引用方式说明

本入口使用明确的读取指令和 Markdown 相对路径链接，不依赖 `@path` 自动导入。
OpenAI 官方文档描述了 AGENTS.md 的逐级发现与合并机制，没有将 `@path` 定义为外部文件导入语法；普通链接本身也不等于内容已自动注入。
`project_doc_fallback_filenames` 是候选文件名配置，每层最多选择一个文件，不是多个规则文件的导入列表。

依据：[OpenAI 官方 AGENTS.md 文档](https://learn.chatgpt.com/docs/agent-configuration/agents-md)（核实日期：2026-09-09）。
