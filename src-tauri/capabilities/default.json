{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-workflow",
  "description": "Allow the main ArgusFlow window to validate and run workflows or read run history.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-show",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "allow-begin-runtime-initialization",
    "allow-get-startup-status",
    "allow-retry-startup",
    "allow-list-runs",
    "allow-get-run",
    "allow-read-run-events",
    "allow-read-run-artifact",
    "allow-validate-workflow",
    "allow-run-workflow"
  ]
}
