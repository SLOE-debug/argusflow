{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-workflow",
  "description": "Allow the main ArgusFlow window to validate and run in-memory workflows.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:default",
    "allow-validate-workflow",
    "allow-run-workflow"
  ]
}

