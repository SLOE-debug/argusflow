> 历史诊断：本文涉及的旧共享 GPU 采样与录制帧选择管线已于 2026-09-17 删除；故障记录保留，当前实现见 [公共变化区域与 Paddle OCR](sampling.md)。

# 2026-09-12 录制状态提示排查

## 19:54 会话与完整帧方案

最新分析会话为 `a2709ca5-c4b0-41d8-a037-2d984452f662`，688条记录：515 Raw、66 Attempt、30 Visual、26 Durability、23 Association、13 Interaction、12 Structure、3 State。只读分析并查看相关PNG，没有修改原录制。

- Win输入的鼠标位置为(796,283)。搜索控件实际边界为[56,463,1017,503]，但UIA命中进程与输入时旧前台进程不同，被标为stale。旧代码回到鼠标邻域，产生原点(316,0)、960×643的偏上裁剪。后续焦点裁剪原点(56,123)、960×720仍没有覆盖完整搜索结果。
- 原始输入26、35、41、52对应O、T、E、P，确实因4个证据任务满而写BudgetExceeded。另有42条NotConfigured（30 OCR、12 CDP），它们是未启用可选能力的噪声，并非42张截图失败。
- 此会话只有Recording、Stopping、Stopped，没有Paused，不能从日志证明暂停后持续计算；代码确认旧共享CaptureService不会随录制暂停关闭，直到桌面宿主退出。

用户确认办公内容不依赖瞬时画面后，录制v2改为独立DXGI完整帧流：15FPS上限，GPU等比缩至1080p范围，CPU短缓存，输入按QPC固定前后图。同窗口文本合并、系统键/点击独立观察；图片编码不再占每个按键一个采集名额。暂停先停止Hook、固定现有帧并关闭DXGI线程，再保存内存图像。未配置OCR/CDP不逐图发NotConfigured。旧数据保持原样。

本机原生来源验证两次2560×1600→1728×1080，首帧243/154ms，关闭30/31ms，线程回收且缓存归零；这是两次生命周期测试，不是持续CPU/GPU性能保证。合成GPU测试验证四种旋转和缩放；确定性队列测试验证两个下游繁忙时80条输入仍全部关联图片。未自动重演用户的真实Windows搜索/Notepad操作。

以下保留此前排查历史，其中旧截图方案的描述不代表v2当前实现。

只读分析截图对应会话 `a2263698-483c-4332-aeee-77e519fcafc0` 的 `events.afr`，按 AFR1 帧边界解析并核对连续序号；未修改、恢复或重新导出该会话。本次分析未校验 BLAKE3 摘要，未启动额外桌面录制。

文件包含 1282 条记录、1083 条原始输入、13 项规范化操作、16 条 UIA 结构记录、31 条图像记录。最后状态为 Paused，原因中报告队列峰值 7、已知输入丢失 0；末条同步记录的水位为 1281。没有 Faulted 状态记录。

## 58 条提示的组成

| 原因 | 条数 | 判断 |
| --- | ---: | --- |
| 未配置 OCR 模型目录 | 31 | 可选能力未启用，被错误计入运行时缺口 |
| 未配置 CDP 端点和 target | 16 | 可选能力未启用，被错误计入运行时缺口 |
| 历史锚点遇到 Accumulated 缺口 | 3 | 采集层因 DXGI 多帧累积记录历史缺口 |
| 历史锚点等待超时 | 1 | `pin_at` 使用 300ms 截止时间，本次未及时取得锚点 |
| 没有可锚定显示来源 | 4 | 上述四次锚定失败的后续汇总，不是另外四次输入失败 |
| 前后图观察没有固定终点，状态 HistoryGap | 3 | 画面历史不连续，无法给出可靠终点 |

扣除可选能力的 47 条记录，仍有 11 条截图缺失记录，涉及 7 条不同原始输入。计数单位是阶段记录，不是失败操作数。历史缺口由 `argusflow-windows/src/capture/output/acquire.rs` 在 `AccumulatedFrames > 1` 时报告。已有数据能确认缺口与超时，不能确定多帧累积具体由 GPU 压力、调度还是其他运行负载造成；也不能重建已缺失的历史帧。

## 本次修正与边界

- 标题栏合并为一个状态入口，移除单独异常按钮和外置停止方块，停止功能进入菜单。
- 可选能力未配置使用明确的 `NotConfigured` 结果并保留原因，不再累加 `evidence_gaps`；真实不可用、超时及失败继续记录。
- 证据缺口显示警告，不等同于输入录制故障。完整性提示按缺失记录条数显示。
- 计数修正适用于更新后的后端所写记录；不按错误字符串重解释或改写已有会话。本次未调整采集性能，也未声称原截图历史缺口已修复。

## 后续：19:20 的 Notepad 链路

只读检查最新会话 `be650a73-f156-4f5d-802a-defdc90e0efc` 的 686 条记录，并实际查看关联 PNG。25 条图片记录由 11 条 Before、9 条 Intermediate、5 条 After 组成；5 条 After 全属于搜索文字输入。搜索窗口切换、打开程序的点击和关闭点击没有取得 After。

- 原始 #3 是搜索窗口切换；图片 #9 与 #12 分别标为前图与过程图，却有相同哈希，实际都是此前的桌面局部。#15 随即报告 HistoryGap 并结束观察。
- 打开程序点击 #253/#257 以及窗口切换 #261 的终点被 HistoryGap 提前取消；随后 Notepad 窗口切换 #272 的前图锚定落在 6.656–6.973 秒的 Accumulated 缺口中，旧代码因此根本没有观察它的后图。
- 关闭点击 #625/#629 同样在 HistoryGap 时结束，只保存前图和早期过程图。
- 日志没有左右 Win 键原始事件。代码检查确认，自身 PID 过滤会在 ArgusFlow 仍为前台时丢弃 Win 键；一般松键也不触发证据任务。这是与现象一致的代码原因，旧日志无法追溯已经过滤的事件。

修正分别覆盖输入过滤、Win 释放观察、前图失败后的独立后图、跨过程缺口的终点等待，以及排除观察区域外重绘的代表帧选择。没有调大固定延时来掩盖问题，也没有将缺口改成完整成功。

新增确定性测试覆盖缺帧后窗口画面落盘、前图缺失但后图存在、没有新帧时拒绝旧图、固定后图不被后续关闭画面替换、区域外变化不成为代表图、Win 过滤与释放归一化。验证使用内存帧源和 PNG 解码，不包含本轮真实 Windows 搜索重录；历史录制保持原样，缺失帧无法补回。
# 2026-09-12 22:27 会话：窗口切换及单字符回看错帧

只读检查会话`7ca1e469-0938-4021-804d-cac83c75c259`：693条完整记录，无尾部半记录。窗口切换Raw #7发生于0.9917秒，Input #44引用0.5492秒旧桌面；After #54引用1.2566秒画面，实际查看附件已出现开始菜单。第一字符Raw #13发生于1.7584秒，其Input #48仍是1.606秒未输入的菜单；下一事件关联的1.941秒图片实际已经出现首字母。默认按Input排序，把操作位置图当成输入/切换结果，是本次直接原因。

普通keyup此前被requires_evidence过滤；连续文本After又在整组空闲时选择，不能用于逐字符结果。修复让普通松键进入观察，逐事件Response在松键后100ms/窗口切换后350ms观察新呈现版本，下一按下或滚轮给出不再前移的时间上界。GPU读回晚于该事件时继续等待原上界内版本，不借用后续字符。无新版本最多等1秒，静态像素不重标成新Response，也不产生“截图失败”噪声。暂停/强制交付仅使用已有缓存。

回看对窗口切换/系统键优先Response或已保存After；文本优先精确raw关联的Response，不使用整组After冒充单字符；鼠标点击保留Input定位帧。已有窗口切换图片可立即正确展示；未保存独立响应的历史文本记录不能由新代码补造。未改写用户录制。验证使用合成帧重演这些时间值，没有操作用户窗口或执行浏览器验收。
