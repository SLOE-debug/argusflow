/** 仅描述 compile_target 接受的中文节点语言；示例由真实解析器测试。 */
export const aqlExamples = [
  '按钮(名称="保存", 可用=是)',
  "文档(聚焦=是)",
  '面板(名称="工具栏") >> 按钮(名称="保存")',
  "列表() > 列表项(文本包含=$条目)",
  '目标((名称="保存" 或 文本="保存") 且 非 可用=否)',
  "文本(文本 匹配 /订单[0-9]+/i)",
  '输入框(名称 开头为 "搜索", 当前值 结尾为 "词")',
  '文本(文本 包含 "订单", 置信度 >= 0.9)',
  '首个(文本(文本="标题"))',
  "第几个(列表项(), 2)",
  "目标(选择=最左上角)",
  '空间查找(锚点=文本(文本="姓名"), 目标=输入框(), 方向=右, 最大距离=200 逻辑像素, 排序=距离从近到远, 选择=第1个)',
  '空间查找(锚点=文本(文本="标题"), 目标=文本(), 对齐=同一行, 对齐容差=范围短边的 2%, 排序=从左到右)',
  '空间查找(锚点=文本(文本="起点"), 目标=文本(), 第二锚点=文本(文本="终点"), 两锚点模式=线段, 带宽=10 逻辑像素)',
];
export const aqlPrompt = `工作流 config.query 使用中文 AQL，由 compile_target 编译。platform 在 config 中单独指定。
角色白名单：目标、窗口、对话框、面板、按钮、输入框、复选框、单选框、组合框、列表、列表项、树、树项、选项卡组、选项卡、菜单、菜单项、链接、图片、表格、行、单元格、文档、文本。
角色后用圆括号，可为空或包含条件。通用文本属性：名称、类型、文本、当前值；布尔属性：可用、可见、聚焦、勾选、选中；数值属性：置信度。类型属性的值使用中文角色名称。
UIA 可使用除置信度以外的上述属性及所有角色；CDP 可使用除置信度以外的上述属性及窗口以外的角色；OCR 使用目标/文本、文本/置信度。属性必须来自实际证据，名称与正文分别核对。
文本运算：=、!=、包含、开头为、结尾为、匹配；布尔运算：=、!=；数值运算：=、!=、>、>=、<、<=。条件组合：非、且、或及圆括号；优先级 非 > 且 > 或，逗号也表示且。
文字值是 JSON 双引号字符串，布尔值是 是/否，数值是有限数字。匹配的右值为 Rust regex 支持的 /模式/ 或 /模式/i。参数写成 $名称，同一参数保持相同类型，并通过 Task.inputs 绑定原值。包含也可写为 文本包含="内容"。
UIA/CDP 层级：A > B 查直接子级，A >> B 查后代，圆括号可分组；关系从左到右结合。
序号：首个(查询)、第几个(查询,正整数)，索引从 1 开始；仅在证据确认顺序语义时选序号。角色查询内 选择=从上到下/从左到右/最左上角 按空间位置选择首个。
空间语法：空间查找(锚点=查询,目标=查询,可选字段...)。以下为支持字段：
方向=右/上右/上/上左/左/下左/下/下右/任意方向；或 角度区间=起始角 度到 结束角 度，角度在 [0,360)，起止不同。方向与角度区间二选一。
距离依据=中心距离/边缘距离；最小距离/最大距离=长度，最小值不大于最大值。
长度：非负数 逻辑像素，或 范围短边的/范围宽度的/范围高度的 非负数%，数值也可用数值参数。
对齐=同一行/同一列 与 对齐容差=长度 成对填写。
范围=查询；区域关系=区域内/区域外 与范围配合；排除重叠=是/否。
第二锚点=查询；两锚点模式=线段 时填写 带宽=长度，矩形模式只填 两锚点模式=矩形。
排序=距离从近到远/距离从远到近/从上到下/从左到右/最左上角；次排序取相同枚举并与排序配合；选择=第N个 或正整数，与排序配合，N 在 1–4096。
锚点与范围需可唯一定位；空间关系使用同一来源及坐标空间。截图矩形与文本偏移分别记录，不互相代替。
以下例子仅说明语法，内容和定位须由本次证据决定：
${aqlExamples.join("\n")}`;
